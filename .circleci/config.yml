version: 2
jobs:
  build:
    machine: true
    working_directory: ../

    steps:
      - checkout
      - run: echo "iniciando build da imagem docker."
      # run: docker build -t alysonfranklin/ansible:$CIRCLE_SHA1

  push:
    machine: true
    working_directory: ../

    steps:
      - run: echo "Enviando imagem para o repositório Docker Hub"
      # run: docker login -u username -p password
      # run: docker push alysonfranklin/ansible:$CIRCLE_SHA1

workflows:
  version: 2
  build-and-push:
    jobs:
      - build
      - push:
          requires:
            - build
